/*
* This source file was generated by the Skeletal project creation tool.
* https://github.com/cbmarcum/skeletal
*/
package {{project_package}}

import geb.spock.GebSpec
import {{project_package}}.pages.GebHomePage
import {{project_package}}.pages.TheBookOfGebPage

class {{cls}} extends GebSpec {

    def "can get to the current Book of Geb"() {
        when: "I go to the homepage"
        to GebHomePage

        and: "I click on the Manuals menu"
        manualsMenu.open()

        then: "the first link is marked as current"
        manualsMenu.links[0].text().startsWith("current")

        when: "I click on the first link"
        manualsMenu.links[0].click()

        then: "I am on the The Book of Geb page"
        at TheBookOfGebPage
    }
}
