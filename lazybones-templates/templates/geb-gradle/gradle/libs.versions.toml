# This source file was generated by the Skeletal project creation tool.
# https://github.com/cbmarcum/skeletal
# https://docs.gradle.org/current/userguide/platforms.html#sub::toml-dependencies-format

[versions]
groovy = "4.0.29"
geb = "8.0.1"
selenium = "4.35.0"
junit-bom = "5.12.0"
junit-platform = "1.12.0"  # matches the bom
spock-reports = "2.5.1-groovy-4.0"

[ libraries]
# Core
groovy = { module = "org.apache.groovy:groovy", version.ref = "groovy" }

# Geb
geb-core = { module = "org.apache.groovy.geb:geb-core", version.ref = "geb" }
geb-spock = { module = "org.apache.groovy.geb:geb-spock", version.ref = "geb" }
geb-junit5 = { module = "org.apache.groovy.geb:geb-junit5", version.ref = "geb" }

# JUnit
junit-bom = { module = "org.junit:junit-bom", version.ref = "junit-bom" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter" }
junit-platform-launcher = { module = "org.junit.platform:junit-platform-launcher", version.ref = "junit-platform" }

# Selenium Drivers
selenium-chrome-driver = { module = "org.seleniumhq.selenium:selenium-chrome-driver", version.ref = "selenium" }
selenium-firefox-driver = { module = "org.seleniumhq.selenium:selenium-firefox-driver", version.ref = "selenium" }

# Spock Reports
spock-reports = { module = "com.athaydes:spock-reports", version.ref = "spock-reports" }
groovy-xml = { module = "org.apache.groovy:groovy-xml", version.ref = "groovy" }
groovy-json = { module = "org.apache.groovy:groovy-json", version.ref = "groovy" }
groovy-templates = { module = "org.apache.groovy:groovy-templates", version.ref = "groovy" }

[plugins]
# (None needed here — we're using the classic id syntax for groovy plugin)
# If you ever switch to a versioned plugin, you can move it here too